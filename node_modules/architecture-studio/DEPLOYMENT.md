# Form + Space — Deployment Guide

## Vercel Deployment

1. Push to Git repository
2. Import in Vercel
3. Framework: Vite
4. Build: `npm run build`
5. Output: `dist`
6. Install: `npm install`

## Manual Build

```bash
npm install
npm run build
```

## Requirements

- Node.js >= 18
